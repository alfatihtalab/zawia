import 'package:flutter/material.dart';

class HeroCard {
  HeroCard({required this.title, required this.color});

  String title;
  Color color;
}


class HeroTopic {
  HeroTopic({required this.headingTitle, required this.topic});

  String headingTitle;
  String topic;
}
